apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: example-apis
  description: A collection of all Backstage example APIs
spec:
  targets:
    - ./all_services/frontend_service.yaml
    - ./all_services/backend_service.yaml
    - ./all_services/new_service.yaml

